//declare variable and using format specifier or placeholder

//variable can not be empty and always contains a (garbage value) before we assign it.

#include<stdio.h>
int main()
{
    int a;
    a = 113;
    printf("%d is an integer",a);
    return 0;
}
